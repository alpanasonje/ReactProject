const CompTwo=({name, message})=>
{
   /*  const nm=props.name;
    const msg=props.message; */

   //const {name, message}=props;

    return(
        <h3>{message} {name}</h3>
    );
}

export default CompTwo;